#include "Flight.h"

/*Flight::Flight() {
    FNumber = 0;
    HeadM = nullptr;
    Trows = 0;
    TColumns = 0;
}

Flight::Flight(int fNumber, int tRows, int tColumns) {
    FNumber = fNumber;
    Trows = tRows;
    TColumns = tColumns;

    // Allocate memory for the 2D array
    HeadM = new int[Trows * TColumns];
    assert(HeadM != nullptr);

    // Initialize all elements of the 2D array to 0
    for (int i = 0; i < Trows; i++) {
        for (int j = 0; j < TColumns; j++) {
            HeadM[i][j] = 0;
        }
    }
}
*/